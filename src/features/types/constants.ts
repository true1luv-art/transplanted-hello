/**
 * Application-level constants.
 *
 * The values live in `@/lib/constants` so that modules (which must never
 * depend on the feature layer) can import them too.
 */
export * from "@/lib/constants";
