// ── Test: activate-offers (placeholder) ─────────────────────────────────────
// NOTE: Placeholder test. Does NOT transact with the Hive blockchain. Mock
// `@/lib/keychain` (transfer) to assert the memo encodes a valid window.

export async function test(): Promise<void> {
  // Example (when mocked): time_ended === time_started + windowHours * 3600.
  console.log('[test:activate-offers] placeholder — not run against blockchain')
}
